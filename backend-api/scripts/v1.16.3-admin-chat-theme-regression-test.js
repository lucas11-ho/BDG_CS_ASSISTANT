import assert from "node:assert/strict";
import fs from "node:fs";
const read=(p)=>fs.readFileSync(new URL(`../../${p}`,import.meta.url),"utf8");
const core=read("backend-api/src/core.js");
const support=read("backend-api/src/support-service.js");
const chat=read("chat-pro/src/App.tsx");
const api=read("admin-pro/src/lib/api.ts");
const buttons=read("admin-pro/src/routes/_admin.action-buttons.tsx");
const menu=read("admin-pro/src/routes/_admin.ai-content-studio.tsx");
const customerService=read("admin-pro/src/routes/_admin.customer-service.tsx");
const nav=read("admin-pro/src/components/AdminLayout.tsx");
const migration=read("backend-api/migrations/041_v1.16.3_admin_chat_theme_and_queue_guard.sql");
const tests=[];
const test=(n,c)=>{assert.ok(c,n);tests.push(n);console.log("PASS",n)};
test("v1.16.3 release marker",core.includes("1.18.2-ai-knowledge-library"));
test("stable Admin platform context is used",api.includes("getPlatformContext")&&!buttons.includes("listSupportPlatforms")&&!menu.includes("listSupportPlatforms"));
test("Menu and Buttons own routes remain independent",menu.includes('api.list("ai-content")')&&buttons.includes('api.list("action-buttons")'));
test("one active AI job is database enforced",migration.includes("uq_ai_jobs_one_active_per_conversation"));
test("backend returns pending conflict",core.includes("CONVERSATION_RESPONSE_PENDING"));
test("support settings cannot re-enable multiple queued questions",support.includes("allow_messages_while_ai_processing:false")&&!customerService.includes('name="allow_messages_while_ai_processing"'));
test("composer disables for every active job",chat.includes("closed || isSending || jobs.length > 0"));
test("customer history is ten-message progressive",support.includes("customerMessageWindow")&&chat.includes("Show previous messages"));
test("Guide and Chat themes are separate",migration.includes("guide_theme_settings")&&migration.includes("chat_theme_settings")&&core.includes("/admin/themes/chat")&&core.includes("/admin/themes/guide"));
test("language selector is removed",!chat.includes("<select value={language}"));
test("buttons are global",buttons.includes("Global Buttons")&&!buttons.includes("label_hi")&&core.includes("label: row.label"));
test("navigation separates appearance and engagement",nav.includes("Guide Theme")&&nav.includes("Chat Theme")&&nav.includes("Global Buttons"));
test("Burmese tester placeholder is removed",!menu.includes("ဗိုက်ဆာ"));
console.log(`\n${tests.length}/${tests.length} v1.16.3 checks passed.`);
