name: v1.17.2 Luke Shared Hosting Production Checks

on:
  push:
    branches: [main, production]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: bdg_integration_test
          POSTGRES_USER: bdg_test
          POSTGRES_PASSWORD: bdg_test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U bdg_test -d bdg_integration_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      VITE_API_BASE: https://api.example.com
      VITE_BDG_API_BASE: https://api.example.com
      VITE_API_BASE_URL: https://api.example.com
      VITE_USE_MOCK: "false"
      VITE_MOCK_MODE: "false"
      TEST_DATABASE_URL: postgresql://bdg_test:bdg_test_password@127.0.0.1:5432/bdg_integration_test
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
          registry-url: https://registry.npmjs.org
          cache-dependency-path: |
            backend-api/package-lock.json
            guide-pro/package-lock.json
            chat-pro/package-lock.json
            admin-pro/package-lock.json
            staff-pro/package-lock.json

      - name: Verify Render backend
        working-directory: backend-api
        run: |
          npm ci --no-audit --no-fund --registry=https://registry.npmjs.org/
          npm run check
          npm run test:regression
          npm run test:prompt-runtime
          npm run test:simplified-ai
          npm run test:support
          npm run test:v1161
          npm run test:v1162
          npm run test:v1163
          npm run test:v1164
          npm run test:v1170
          npm run test:v1171
          npm run test:v1171r2
          npm run test:v1172
          npm run test:v1172r2
          npm run test:v1173
          npm run test:v1174
          npm run test:v1174r1
          npm run test:v1174r2
          npm run test:v1174r3
          npm run test:v1174r4
          npm run test:v1180r1
          npm run test:v1181-ai-knowledge
          npm run test:v1182-ai-knowledge
          npm run test:v1183-frontend
          npm run test:v1184-bulk-content
          npm run test:chat-reliability
          npm run test:security
          npm run test:structured
          npm run test:upload
          npm run test:integration
          npm audit --audit-level=high

      - name: Build Guide Pro
        working-directory: guide-pro
        run: |
          npm ci --no-audit --no-fund --registry=https://registry.npmjs.org/
          npm run typecheck
          npm run build
          npm audit --audit-level=high

      - name: Build Chat Pro
        working-directory: chat-pro
        run: |
          npm ci --no-audit --no-fund --registry=https://registry.npmjs.org/
          npm run typecheck
          npm run build
          npm audit --audit-level=high

      - name: Build Admin Pro
        working-directory: admin-pro
        run: |
          npm ci --legacy-peer-deps --no-audit --no-fund --registry=https://registry.npmjs.org/
          npm run test:customer-service-route
          npm run typecheck
          npm run build
          npm audit --audit-level=high

      - name: Build Staff Pro
        working-directory: staff-pro
        run: |
          npm ci --no-audit --no-fund --registry=https://registry.npmjs.org/
          npm run typecheck
          npm run build
          npm audit --audit-level=high
